# 后台修改内容教程（文字 / 视频 / 图片 / 音乐）

后台地址：**`你的网址.netlify.app/admin`**，用你注册时设的邮箱 + 密码登录。

> 一句话流程：登录后台 → 左边选要改的内容 → 改 → 点右上角 **Save（保存）** → 等 1~2 分钟 → 网页按 **Ctrl + F5** 强制刷新，就能看到新内容。

---

## 一、改文字（名字、简介、经历、联系方式…）

1. 登录后台后，左边栏点 **「站点文字与设置」**
2. 里面只有一项「全局内容」，点进去就是所有文字的表单
3. 找到要改的字段直接改，对照表：

| 网页上显示的位置 | 后台字段名 |
|---|---|
| 大标题名字 | 名字（中文）/ 名字（英文装饰） |
| 名字下面那行职业 | 职业一句话 / 职业（英文装饰） |
| 签名语"把每一帧…" | 签名语 |
| 首页小徽章"中国 · 求职中" | 所在地 · 状态 |
| 3D 手账三个页签里的介绍 | 手账页签介绍（AE动画页 / 角色IP页 / 毕业设计页） |
| 「角色面板」的两段简介 | 关于我（每段一条，可加减条数） |
| 「角色面板」的技能条 | 技能条（软件名 + 等级 0~100） |
| 「角色面板」的技能徽章 | 软件徽章 |
| 「剪辑时间线」的经历卡片 | 经历时间线（时间段/标题/公司/简介/详情/标签，可加减；「简介」显示在卡片上，「详情」和「标签」显示在点开的大弹窗里） |
| 「保持联系」区的邮箱电话微信 | 联系方式 |

4. 改完点右上角 **Save**。

---

## 二、改作品（视频票根卡）

1. 左边栏点 **「作品（视频卡）」** → 点「作品列表」
2. 每条作品是一张票根，字段对照：

| 票根上显示的内容 | 后台字段 |
|---|---|
| 黑色区域的封面图 | 封面图 |
| 票根标题 | 标题 |
| 标题下面的英文小字 | 英文名 |
| 分类角标（动效/特效/动画/其他） | 分类（下拉选） |
| 年份、时长 | 年份、时长（格式写成 00:45 这样） |
| 弹窗里的介绍/职责/软件 | 介绍 / 我的职责 / 使用软件 |
| 弹窗里播放的视频 | 视频文件（mp4） |

- **新增作品**：拉到列表最下面，点 **「+ Add 作品」**，把字段填齐
- **删除作品**：点那张作品卡片右上角的 **×**
- **调顺序**：直接**拖动**条目左侧的手柄排序
- ⚠️ **ID 字段**：每个作品要有一个不重复的英文 ID（比如 w9、w10），新建的默认会带一个，别和已有的重复就行

## 三、传视频

作品视频传到后台的素材库：

1. 左边栏点 **「Media」（素材库）**
2. 把 mp4 文件**拖进去**（或点 Upload 选文件）
3. 传完点那个文件，复制它的地址（形如 `/assets/uploads/xxx.mp4`）
4. 回到「作品」里，把这个地址填进对应作品的 **视频文件** 字段
5. Save

> 不传视频的话，票根弹窗会显示封面图 + 自动走一遍模拟播放进度条，也不会难看。

**视频建议**：1080p、mp4、压到 20MB 以内（剪映导出时码率选 4Mbps 左右就够清晰）。太大 HR 打开会卡。

## 四、传图片（封面图 / 明信片）

和视频一样走素材库：

- **作品封面图**：传到素材库后，在作品的「封面图」字段点 **Choose image** 直接选
- **明信片图片**：左边栏 **「明信片堆」** → 选一堆 → 每张卡片有「图片 + 图注」，图片点 Choose image 从素材库选
  - 一堆最多显示前几张，建议每堆放 3~5 张
  - 新增一堆：拉到列表底部 **「+ Add 明信片堆」**

**图片建议**：封面图按 1920×1080（横版）准备；明信片图片竖版横版都行，单张 2MB 以内。

## 五、简历 PDF

1. 素材库上传你的 `resume.pdf`
2. 到「站点文字与设置」→「联系方式」→ **简历 PDF 链接** 填它的地址
3. 首页「下载简历 PDF」按钮就会变成可下载的
4. 没填链接时按钮显示"整理中…"，不会露出破绽

## 六、背景音乐和音效

在「站点文字与设置」最下面两个字段：

- **背景音乐 mp3 链接**：素材库传一首 mp3 → 复制地址填进来。填了之后：访客打开网页就**自动播放**（浏览器要求访客先点一下页面才会出声，这是所有网站的通用限制）；右上角「音乐」按钮可以手动关；打开作品放映厅看视频时音乐会**自动淡出**，关掉弹窗后自动淡入回来
- **音效默认开启**：打勾 = 访客打开网页时翻页/点击音效默认是开的；不打勾 = 默认静音（访客可以自己开）

## 七、每次改完之后的确认动作

1. 后台右上角 **Save**
2. 等 1~2 分钟（Netlify 在后台重新打包你的网站）
3. 打开你的网址，按 **Ctrl + F5** 强制刷新
4. 还没变？去 Netlify 控制台看项目主页最新的 deploy 是不是绿色 Published，是绿色再强刷一次

---

## 几个防踩坑提醒

- ❌ 不要改作品的 **ID** 字段（改了点赞数会对不上）
- ❌ 分类只能从 动效/特效/动画/其他 里选，不要自己打字
- ✅ 改错了随时可以回后台再改回来，不会搞坏网站
- ✅ 内容全存在你的 GitHub 仓库里，就算 Netlify 挂了内容也丢不了
- 🔒 别人没有你的邮箱+密码，进不了后台改不了东西