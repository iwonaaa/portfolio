# 作品集网站 · 上线与后台使用说明

这份文档面向完全不会写代码的你。跟着点就行，全程免费。

---

## 一、把网站弄上线（一次性，约 15 分钟）

需要两个免费账号：GitHub（存代码）和 Netlify（托管网站）。

### 第 1 步：注册 GitHub
1. 打开 https://github.com → Sign up → 用邮箱注册（免费）。
2. 验证邮箱，完成注册。

### 第 2 步：把网站文件传上 GitHub
1. 登录后，右上角 **+** → **New repository**。
2. 名字填 `portfolio`（或任意英文名），选 **Public**，其他都不用动，点 **Create repository**。
3. 创建后点页面上的 **uploading an existing file** 链接。
4. 在电脑上打开文件夹 `outputs/portfolio`，**全选里面所有文件和文件夹**（index.html、admin、assets、content、css、js、netlify.toml），**直接拖进网页**的拖放区。
5. 等上传完，点页面底部绿色 **Commit changes**。

> 注意：拖进去的第一层就应该是 index.html，不要再多套一层文件夹。

### 第 3 步：用 Netlify 发布
1. 打开 https://www.netlify.com → **Sign up** → 选 **GitHub** 账号直接登录（免注册）。
2. 首页点 **Add new project** → **Import an existing project**。
3. 选 **GitHub**，授权后找到你的 `portfolio` 仓库。
4. 构建设置全部留默认（Netlify 会自己读 netlify.toml），点 **Deploy**。
5. 等 1~2 分钟，顶部会出现你的网址，形如 `https://随机词.netlify.app`。
6. 想改网址：Site configuration → Change site name，改成比如 `xu-portfolio.netlify.app`。

**这个网址就是发给 HR 的链接。**

### 第 4 步：开通后台登录权限（上传/改内容需要密码）
1. Netlify 项目页 → 左侧 **Access & security** → **Identity** → 点 **Enable Identity**。
2. 同一页找到 **Git Gateway** → **Enable Git Gateway**。
3. 回到 **Identity** 页 → **Invite users** → 输入你自己的邮箱 → 去邮箱点邀请链接设密码。

### 第 5 步：进后台
打开 `https://你的网址.netlify.app/admin` → 用刚设的邮箱+密码登录。这就是你的后台。

---

## 二、后台都能改什么

后台左边有三栏内容，改完点右上角 **Publish**（发布），等 1~2 分钟网站自动更新。

### 站点文字与设置
- 名字、职业一句话、签名语、所在地状态
- 手账页签介绍（首页 3D 手账里 AE动画/角色IP/毕业设计 三页的文字）
- 关于我（角色面板的两段简介）
- 技能条（软件名 + 等级 0-100）
- 软件徽章
- 经历时间线（每条：时间段、标题、公司、描述）
- 联系方式（邮箱、电话、微信、简历 PDF 链接、B站主页）
- **背景音乐**：填一个 mp3 链接（先在后台左侧 Media 素材库里上传 mp3，再把地址复制进来），填了之后网页右上角会出现"音乐"开关按钮
- **音效默认开启**：勾选后访客打开网页默认有翻页/点击音效（不勾选则默认静音，访客可以自己开）

### 作品（视频卡）
每条作品是一张"电影票根"：
- 标题 / 英文名 / 分类（动效/特效/动画/其他）/ 年份 / 时长
- 我的职责、使用软件、介绍文字
- **封面图**：票根上黑色的预览区，后台直接上传图片
- **视频文件**：上传 mp4 后，点票根弹出的放映厅就能播放；不上传则显示封面图 + 模拟播放预览
- 初始点赞数（只是个起始数字，真实点赞存在访客自己浏览器里）
- 新增作品：点 **New 作品**；删除：条目右上角菜单

### 明信片堆（图片作品）
- 每一堆：标题、英文名、若干卡片（图片 + 图注）
- 用于展示海报、手绘、设定集这类图片作品

### 素材库（Media）
后台左侧 **Media** → 上传图片/视频/简历 PDF。上传后复制它给的地址，填到对应字段里。
- 作品封面图、视频、明信片图片都在这里传
- 简历 PDF 传完后，把地址填到「联系方式 → 简历 PDF 链接」

---

## 三、一些不能在后台上改的东西（找文件换）

| 想换什么 | 换哪个文件 |
|---|---|
| 整册作品集 PDF（手账毕业设计页翻的那个） | 把 `assets/portfolio.pdf` 替换掉 |
| 联系区背景插画 | 替换 `assets/contact-bg.png` |
| 手绘字体/颜色风格 | `css/style.css`（这个建议找我改） |

---

## 四、常见问题

**Q: 改了后台内容，网页没变？**
A: 后台点了 Publish 之后，Netlify 要 1~2 分钟重新构建。刷新网页时按 Ctrl+F5 强制刷新。

**Q: 点赞和评论数据会同步吗？**
A: 不会。它们存在每个访客自己的浏览器里（别人看不到你点的赞）。想要全站同步的点赞需要数据库，以后需要再加。

**Q: 视频太大传不上？**
A: 建议先用剪映/HandBrake 压到 20MB 以内（1080p、码率 4Mbps 左右足够清晰）。

**Q: 后台网址 /admin 打不开或一直转圈？**
A: 确认 Netlify 的 Identity 和 Git Gateway 都已开启（第一部分第 4 步），并且你是被邀请的用户。

**Q: 想自己先在本机预览？**
A: 双击 index.html 打不开内容（浏览器安全限制）。在 `hr` 文件夹里运行：`node work/serve.mjs outputs\portfolio 4173`，然后访问 http://localhost:4173/

---

有问题随时叫我。